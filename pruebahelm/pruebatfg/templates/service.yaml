apiVersion: v1
kind: Service
metadata:
  name: {{ include "pruebatfg.fullname" . }}
  labels:
    {{- include "pruebatfg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pruebatfg.selectorLabels" . | nindent 4 }}
